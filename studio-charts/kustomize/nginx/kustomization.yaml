apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonLabels:
  app.kubernetes.io/managed-by: argocd

namespace: default

# release.yaml is generated by kustomized-helm pugin and is required
resources:
- ./release.yaml